# Agent-notify 前端 UI/UX 深度重构报告（第二轮 · 新思考重交付）

> **性质**：纯 UI/UX 重构（视觉系统 / 信息架构 / 布局范式 / 双主题 / 无障碍），业务逻辑与数据结构零改动。
> **方式**：主编排 + 10 个子代理阶段流水线（统一 `opencode-go/deepseek-v4.1-flash`），每阶段独立复验后提交。
> **背景**：第一轮交付已由 `39e7b61` 主动 revert（"留待 UI 迭代重新交付"）；本轮按新思考从零重做，**以渠道为主**。
> **设计权威**：`ui-visual-composition` / `information-architecture-navigation` / `office-web-ui-system` 三份 skill 方法论。
> **规格**：`docs/superpowers/specs/2026-09-24-ui-ux-redesign-design.md` · **计划**：`docs/superpowers/plans/2026-09-24-ui-ux-redesign-plan.md` · **决策日志**：`apps/desktop-ui/UI_DECISIONS.log`

---

## 〇、结论速览

1. **六页全部迁移到新范式**，且**渠道优先**贯穿 IA、空态、KPI 与实施顺序。
2. **明暗双主题一等公民**：亮色专门设计（非反色：白面+细边+淡阴影、深青主色、零辉光）；导航底部「明/暗/跟随系统」切换 + 记忆，默认跟随系统。
3. **质量证据全绿**：`typecheck` / `vitest 65` / `build` / **a11y 14（light 7 + dark 7）** / **visual 12（6 页 × 2 主题）** / `e2e 39` / `go test ./...` / `go vet` / `gofmt` / `tools\lint.ps1`（版本一致性含）。
4. **业务红线零越界**：bridge/数据/请求/缓存 Key/表单提交/路由路径/状态机/modern 虚拟滚动参数零改动；可访问名与 role 零改动。
5. 收尾阶段抓出并修复 **3 处真实缺陷**：暗色下不支持字段底 1.73:1、InlineError 底 1.04:1（均为旧硬编码浅底）、「无可见错误」红字语义色误用。
6. **唯一待办**：在真实桌面窗口做最终人工验收（§7）。

## 一、交付清单（11 个提交）

| 提交 | 阶段 | 内容 |
| --- | --- | --- |
| `1530dda` | 1+2 | 双主题 token + 表面语义层 + 主题切换 + 渠道优先导航 |
| `782ee6f` | — | 计划偏差修正（colorScheme 过渡策略） |
| `32b8158` | 3 | 范式组件六件套 + 骨架屏 + 语义字阶（28/18/14/12） |
| `7e6933f` | 4 | **渠道页**（第一优先）：卡带 + 账号行卡 + 空态漏斗 |
| `ffe3654` | 5a | Agent 管理：行卡列表 + 三段详情 |
| `b455f2b` | 5b | 总览：KPI 锚点带（渠道健康居首）+ 投递流分组 |
| `98d1253` | 5c | 历史：筛选收拢 + 固定轨道 + 虚拟列表防白闪 |
| `5d00fff` | 5e | 设置：左目录 + 分组卡 + 一决策一行 + 吸底保存 |
| `b380b83` | 5d | 诊断：严重度分组 + 动作行 + 迁移卡独立 |
| `7084838` | 6a | 死代码清扫 + 空态引导一致性 + **暗色矩阵接入** |
| 本提交 | 6b | 历史语义色修复 + **双主题基线矩阵刷新** + 文档收官 |

**覆盖面**：6 个 CSS 文件全量重构 + `patterns.css` 新建 + 6 个范式组件新建 + `theme.ts`/`ThemeSwitcher` 新建 + 六页 features 全部迁移 + `playwright.config`（dark 项目）+ 12 张双主题基线。

## 二、双主题与设计系统

- **架构**：语义角色 token 不变；**表面语义层** `--surface-1/2/3` + `-blur/-border/-shadow` + `--hairline` + `--elevation-1/2/3`，组件零分支换肤。
- **暗色**：Cyberpunk 基线按气质 B 收敛发光（白名单：主按钮微光/hover/聚焦环/异常徽标呼吸档位 1）。
- **亮色（专门设计）**：`#f5f7fa` 画布 / 纯白面 / 冷灰细边 / elevation 阴影 / **深青主色 `#0e7490`** / 柔和底加深 / 零玻璃零辉光按下不表。
- **切换**：`agentnotify.theme` localStorage ∈ {dark,light,system}；system 跟随 `prefers-color-scheme` 并监听变化；首帧前在 `App.tsx` 模块作用域应用（防闪变）。
- **对比度**：两主题逐对核算（正文 ≥4.5:1；最紧 4.53:1），axe 双主题 14 条全绿。

## 三、六页范式与关键改造

| 页面 | 范式 | 关键点 |
| --- | --- | --- |
| **渠道**（第一优先） | Master–Detail + 强引导 | 因果式页头；渠道卡带；账号行卡（tr 语义保留）；右详情四段；**空态漏斗「先连接渠道」+ 三步**；「添加渠道账号」全页唯一可点（strict 模式） |
| Agent 管理 | Master–Detail | 行卡列表（6 列→4 列）；详情三段（身份 FieldRow / 能力胶囊 / 配置） |
| 总览 | Dashboard | **KPI 锚点带（渠道健康居首）**；例外面板以「待处理」KPI 补偿；投递流今天/昨天/更早分组 |
| 历史 | Filter + Table | 筛选主/次两行 + 重置行尾；固定轨道 + 中文 keep-all；虚拟列表只调 CSS；详情与列表底对齐；修复列轨道越入旧缺陷 |
| 诊断 | Exception Triage | 按严重度分组（错误/等待/正常）；动作行右对齐成列；迁移卡独立成组 |
| 设置 | Settings | **左目录（sticky 锚点）+ 分组卡**；一决策一行（控件右对齐成列）；吸底常驻保存条 |

## 四、质量证据（最终独立复跑）

```
typecheck               ✓
vitest --run            ✓ 10 files / 65 tests passed
build                   ✓ 1999 modules transformed
test:a11y               ✓ 14 passed（desktop light 7 + dark 7）
test:visual             ✓ 12 passed（6 页 × 2 主题基线，3 连跑稳定）
test:e2e                ✓ 39 passed
go test ./...           ✓ 全部 ok
go vet ./...            ✓ 干净
gofmt -l cmd internal   ✓ 无输出
tools\lint.ps1          ✓ 语法/规则/版本一致性 2.0.7 全过
```

## 五、过程自主决策精选（全量见 UI_DECISIONS.log）

1. **亮色非反色**：白面+细边+淡阴影替代玻璃；发光收敛为描边/阴影；主色换深青（亮青在白底对比度不合格）。
2. **KPI 带无标题**：保 h2 顺序契约（运行状态居首），28px 数字成为唯一视觉锚点。
3. **呼吸微光档位 1**：仅 warning/danger（伪元素仅动画 opacity；暗色辉光、亮色极淡阴影；reduced-motion 全关）。
4. **间距即分组**：组内 8px、组间 24~32px；设置「谁跟谁一类」通过卡片容器 + 悬殊间距根治。
5. **空态漏斗**：历史/总览空态补因果文案 + 「去连接渠道」CTA；诊断补因果句。
6. **暗色矩阵首跑抓缺陷**：修复 `#fff8e8`（1.73:1→7.52:1）与 `#fbefed`（1.04:1→12.30:1）两处旧硬编码。
7. **语义色纠偏**：历史「无可见错误」由 danger 红改弱化灰（复用既有 tone 判定，不新增第二真相）。
8. **基线刷新防失真**：`maxDiffPixelRatio` 会掩盖小面积修复 → 强制 `--update-snapshots=all` + 像素直方图核验。
9. **死代码清扫**：8 组零引用规则删除（grep 取证），有引用一律保留。
10. 全程**文件段所有权 + 精确编辑**，主编排每阶段以 git 指纹校验越界（选择器扫描/改动行数守卫）。

## 六、已知偏差与待办（透明记录）

1. **总览例外面板未前置**（测试顺序断言钉死）→ 已用「待处理」KPI 补偿；如需真正前置，改 1 条断言 + 重排，10 分钟。
2. **渠道详情极端长标题逐字换行**（~305px 栏内，测试契约通过）→ 留待「卡头长标题策略」专项（可加 `-webkit-line-clamp` 或横向布局）。
3. **空态 CTA 仅历史/总览**（诊断仅文案）；若六页统一漏斗还要更彻底，可后续统一 EmptyFunnel。
4. `styles/global.css` 为全仓零引用死文件（历史遗留），本轮按纪律未删。
5. 未 push、未打 tag、未发版；`VERSION` 保持 2.0.7。

## 七、人工验收建议（唯一待办）

在真实 Tauri 桌面窗口（1280×800 / 最大化 / 640×400 矮窗各一遍，明暗各切一次）确认：毛玻璃在真实窗口合成下的观感、渠道优先导航与 Tab 顺序、空态漏斗引导、状态徽标呼吸、设置页左目录跳转与吸底保存条。自动化矩阵已全绿，但真实窗口观感仍需人眼验收。

## 八、补记：收官后目检修补（b2d2aed）

在用户的双主题预览验收中又目检抓出并修复 3 处细节：① 导航主题开关「系统」段因 flex 等分（58px/段）截断为「系…」→ 选项改按内容定基宽（`flex: 1 1 auto`，紧凑模式不受影响）；② 渠道漏斗描述在 72ch 上限处句中折行（拆开「真的」）→ `max-width: 88ch` + `text-wrap: pretty`，宽屏一行放下、窄屏自然折行；③ 总览 KPI 首卡标签过长（「渠道账号在线 · 等待登录 0 · 失效 0」）→ 收短为「渠道账号在线」+ 条件异常文案（失效/等待登录 >0 时保留信号）。

12 张双主题基线随之全部重刷（`--update-snapshots=all`）并 3 连跑稳定（14 passed ×3）；a11y 14 passed；e2e 39 passed。另完成收官清理：决策中转目录合并后删除（8fc8c6a）。**本轮共 18 个提交**（4 文档 + 14 代码/清理/修补）。
